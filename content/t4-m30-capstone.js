/* Tier 4 · Module 30 — The Capstone */
(function () {
  const L = a => a.join("\n");

  PQ.defineModule({
    id: "m30", tier: 4, order: 30, icon: "👑",
    title: "The Capstone",
    blurb: "Build a complete system from a specification — domain model, storage, rules, reporting and your own tests.",
    intro: L([
      "This is the last module, and it is deliberately different. There is very little teaching here.",
      "",
      "You are given a specification and asked to build the system, the way you would be at work: with a",
      "domain model that cannot hold invalid data, a storage layer, pure business rules, a reporting layer,",
      "and finally a test suite of your own that has to prove it catches real bugs.",
      "",
      "Everything you need is something you have already done. The difficulty is that nothing tells you",
      "which module it came from."
    ]),
    concepts: [
      { id: "capstone-modelling", name: "modelling a domain", importance: 1.6 },
      { id: "capstone-layers", name: "layering a system", importance: 1.6 },
      { id: "capstone-rules", name: "encoding business rules", importance: 1.5 },
      { id: "capstone-testing", name: "proving it works", importance: 1.6 }
    ],

    lessons: [
      {
        id: "m30l1", title: "How to approach a specification", minutes: 10,
        concepts: ["capstone-modelling", "capstone-layers"],
        content: L([
          "## You have done all of this before",
          "",
          "The capstone that follows is a lending library. Before you start it, this lesson is about the",
          "*method* — because being handed a specification and asked to build the thing is the actual job,",
          "and there is a way to do it that works.",
          "",
          "## 1. Find the nouns",
          "",
          "Read the specification and list the things it talks about: **books**, **members**, **loans**.",
          "Those are your types. Each gets a class or a dataclass, and each is responsible for refusing to",
          "exist in an invalid state.",
          "",
          "Then find the verbs: **borrow**, **return**, **report**. Those are your operations, and they",
          "usually belong to a service that coordinates the nouns rather than to the nouns themselves.",
          "",
          "## 2. Decide the layers before writing anything",
          "",
          "```text",
          "domain      Book, Member, Loan          knows nothing about storage",
          "rules       due dates, fines, limits    pure functions, no I/O",
          "storage     the database                knows nothing about why",
          "service     borrow(), return_book()     coordinates the three above",
          "reporting   formatted output            knows nothing about the database",
          "```",
          "",
          "The dependency arrows all point **inwards**: reporting and storage depend on the domain, and the",
          "domain depends on nothing. If you find yourself importing the database inside a fine calculation,",
          "a layer has leaked.",
          "",
          "## 3. Make the rules pure",
          "",
          "Every rule that can be a function of plain values should be:",
          "",
          "~~~py",
          "def fine_for(day_due, day_returned, per_day=50, cap=2000):",
          "    ...",
          "~~~",
          "",
          "No database, no clock, no printing. You can test the boundary — returned exactly on the due day —",
          "in one line, and the same function serves the service layer, the report and the API.",
          "",
          "## 4. Inject anything that varies",
          "",
          "The current day is the classic example. A system that calls `date.today()` deep inside a rule cannot",
          "be tested at all, because you cannot make it be a different day. Pass the day in.",
          "",
          "This capstone uses plain integer day numbers rather than real dates, for exactly that reason: the",
          "rules are about arithmetic, and dates would add noise without adding difficulty.",
          "",
          "## 5. Build one stage at a time, green before moving on",
          "",
          "Each stage of the capstone has its own checks. Do not try to hold the whole design in your head and",
          "write it in one pass — that is how you end up with six bugs at once and no idea which is which.",
          "",
          ":::tip When you get stuck",
          "Write the smallest version that could possibly pass the first check, run it, then extend. A stage",
          "that is half working and green on three checks is worth far more than a complete design that runs",
          "nothing.",
          ":::",
          "",
          "## What the final stage asks of you",
          "",
          "The last stage does not ask you to write more system code. It asks you to write a **test suite** and",
          "then proves it works by running it against deliberately broken implementations.",
          "",
          "A suite that passes the correct code and also passes the broken code is worthless — and until you",
          "have watched that happen, it is hard to really believe. That is the point of ending here."
        ]),
        exercises: [
          {
            kind: "quiz", title: "Which layer", difficulty: 3,
            concepts: ["capstone-layers"],
            prompt: "Where does \"a member may have at most 3 books on loan\" belong?",
            choices: [
              "In the database schema only",
              "In the rules or service layer, as a check with a clear error — the database cannot express it well and the UI must not be the only guard",
              "In the reporting layer",
              "In the Book class"
            ],
            answer: 1,
            explain: "It is a business rule about a member's loans, not a property of a single row. Enforcing it only in the UI means any other caller bypasses it; expressing it in SQL is awkward and hides it from readers."
          },
          {
            kind: "quiz", title: "Why inject the day", difficulty: 2,
            concepts: ["capstone-rules"],
            prompt: "Why should `is_overdue` take the current day as a parameter rather than reading the clock?",
            choices: [
              "It is faster",
              "So the boundary cases can be tested deterministically instead of only on the right day",
              "Because reading the clock is not allowed in Python",
              "To save memory"
            ],
            answer: 1,
            explain: "A rule that reads the clock can only be tested by waiting. Passing the day in makes every case — the day before, the due day, the day after — a one-line test."
          },
          {
            kind: "code", title: "Warm-up: the fine rule", difficulty: 3,
            concepts: ["capstone-rules"],
            prompt: L([
              "Before the capstone proper, implement the rule you will need in it.",
              "",
              "`fine_for(day_due, day_returned, per_day=50, cap=2000)` returns the fine in pence:",
              "",
              "- returned on or before the due day → `0`",
              "- otherwise `per_day` pence for each day late",
              "- never more than `cap`",
              "",
              "Days are plain integers. This is pure arithmetic — no clock, no I/O."
            ]),
            starter: "def fine_for(day_due, day_returned, per_day=50, cap=2000):\n    ",
            hints: [
              "`days_late = day_returned - day_due`",
              "Return 0 when that is zero or negative.",
              "`min(days_late * per_day, cap)`"
            ],
            tests: [
              { name: "returned early", call: "fine_for(100, 95)", expect: 0 },
              { name: "returned exactly on the due day", call: "fine_for(100, 100)", expect: 0 },
              { name: "one day late", call: "fine_for(100, 101)", expect: 50 },
              { name: "five days late", call: "fine_for(100, 105)", expect: 250 },
              { name: "capped", call: "fine_for(100, 200)", expect: 2000 },
              { name: "the cap can be changed", call: "fine_for(100, 200, cap=500)", expect: 500 },
              { name: "the rate can be changed", call: "fine_for(100, 102, per_day=10)", expect: 20 },
              { name: "never negative", code: "assert all(fine_for(100, d) >= 0 for d in range(80, 120))" }
            ],
            solution: L([
              "def fine_for(day_due, day_returned, per_day=50, cap=2000):",
              "    days_late = day_returned - day_due",
              "    if days_late <= 0:",
              "        return 0",
              "    return min(days_late * per_day, cap)"
            ]),
            takeaway: "Three of those eight checks are boundaries: early, exactly on time, one day late. That is where the bugs live, and it is where you should start writing tests in the final stage."
          }
        ]
      }
    ],

    checkpoint: {
      id: "m30cp", pass: 0.8,
      title: "Checkpoint: Ready for the Capstone",
      brief: L([
        "A short readiness check drawing on the whole journey. Five questions, no hints.",
        "",
        "Pass it and the Capstone unlocks."
      ]),
      items: [
        {
          kind: "code", title: "Everything at once", difficulty: 4,
          concepts: ["dict-group", "sort-key", "error-design"],
          prompt: L([
            "Write `summarise(records)` taking a list of `'name:score'` strings and returning a dictionary",
            "`{'valid': n, 'invalid': m, 'best': name_or_None}`.",
            "",
            "A record is invalid if it does not split into exactly two parts on `:`, or the score is not a",
            "whole number. `best` is the name with the highest score, first-seen wins on a tie."
          ]),
          starter: "def summarise(records):\n    ",
          tests: [
            { name: "normal case", call: "summarise(['ada:90', 'bo:70'])", expect: { valid: 2, invalid: 0, best: "ada" } },
            { name: "counts invalid rows", call: "summarise(['ada:90', 'broken', 'bo:x'])", expect: { valid: 1, invalid: 2, best: "ada" } },
            { name: "empty input", call: "summarise([])", expect: { valid: 0, invalid: 0, best: null } },
            { name: "all invalid", call: "summarise(['a', 'b'])", expect: { valid: 0, invalid: 2, best: null } },
            { name: "first wins a tie", call: "summarise(['a:50', 'b:50'])['best']", expect: "a" }
          ]
        },
        {
          kind: "quiz", title: "The most valuable habit", difficulty: 3, concepts: ["separation"],
          prompt: "Which single habit does most to keep a codebase testable?",
          choices: [
            "Writing more comments",
            "Keeping business logic pure — taking values, returning values, touching no I/O",
            "Using classes everywhere",
            "Adding type hints to every local variable"
          ],
          answer: 1,
          explain: "Pure logic needs no setup, no database, no mocking and no clock. Everything else in testing gets easier once the rules are separated from the machinery around them."
        },
        {
          kind: "predict", title: "One last trap", difficulty: 4, concepts: ["mutable-default"],
          prompt: "What does this print?\n\n~~~py\ndef add(item, seen=[]):\n    seen.append(item)\n    return len(seen)\n\nprint(add('a'), add('b'), add('c'))\n~~~",
          choices: ["`1 1 1`", "`1 2 3`", "`3 3 3`", "An error"],
          answer: 1,
          explain: "The default list is created once, when `def` runs, and shared by every call that omits the argument. Arguments are evaluated left to right before `print` runs, so the lengths are 1, 2, 3."
        },
        {
          kind: "code", title: "Safe and fast", difficulty: 4, concepts: ["structure-choice", "sql-params"],
          prompt: L([
            "Write `matching(records, wanted)` returning the records whose `id` is in `wanted`, in the original",
            "order. `wanted` may be very large — the checks include a big input."
          ]),
          starter: "def matching(records, wanted):\n    ",
          tests: [
            { name: "filters", call: "matching([{'id': 1}, {'id': 2}], [2])", expect: [{ id: 2 }] },
            { name: "empty wanted", call: "matching([{'id': 1}], [])", expect: [] },
            {
              name: "fast on large input",
              code: "import time\nrecords = [{'id': i} for i in range(30000)]\nwanted = list(range(15000, 45000))\nstart = time.perf_counter()\nassert len(matching(records, wanted)) == 15000\nassert time.perf_counter() - start < 2.0, 'convert wanted to a set once'"
            }
          ]
        },
        {
          kind: "quiz", title: "Finishing", difficulty: 2, concepts: ["capstone-testing"],
          prompt: "What makes a test suite genuinely valuable?",
          choices: [
            "It has 100% line coverage",
            "It fails when the code is wrong",
            "It runs quickly",
            "It has many test cases"
          ],
          answer: 1,
          explain: "Coverage, speed and count are proxies. The only property that matters is whether the suite goes red when someone breaks something — which is why the capstone's final stage checks exactly that."
        }
      ]
    },

    project: {
      id: "m30proj",
      title: "Capstone: The Lending Library",
      xp: 1200,
      filename: "library.py",
      concepts: ["capstone-modelling", "capstone-layers", "capstone-rules", "capstone-testing", "dataclass", "sqlite-connect", "dependency-injection", "raise"],
      brief: L([
        "**The final build.** A complete lending library, from a specification, in six stages.",
        "",
        "There are no hints on the later stages and no reference solution shown. Everything you need you have",
        "done before — but nothing here tells you which module it came from.",
        "",
        "## The system",
        "",
        "```text",
        "domain     Book, Member                validated, immutable",
        "rules      due_day, fine_for, is_overdue    pure functions",
        "storage    SQLite: books, members, loans",
        "service    Library.borrow / return_book     coordinates the above",
        "reporting  Library.report                    formatted output",
        "tests      yours, and they must catch real bugs",
        "```",
        "",
        "## Conventions used throughout",
        "",
        "- **Days are integers.** Day 100 is a hundred days after some epoch. This keeps the rules about",
        "  arithmetic rather than calendars, and makes every boundary testable.",
        "- **Money is pence**, always an `int`. Never a float.",
        "- **Errors are exceptions**, never `None` sentinels or printed messages.",
        "- The database is in memory unless a path is given, so every check starts clean.",
        "",
        ":::warn Read each stage in full before writing code",
        "The specifications are precise, including the boundaries. Most failures at this level are from",
        "skimming, not from difficulty.",
        ":::"
      ]),
      starter: L([
        "\"\"\"The Lending Library — PyQuest capstone.\"\"\"",
        "import sqlite3",
        "from dataclasses import dataclass",
        "",
        "",
        "# ---------------------------------------------------------------- domain",
        "",
        "@dataclass(frozen=True)",
        "class Book:",
        "    ...",
        "",
        "",
        "@dataclass(frozen=True)",
        "class Member:",
        "    ...",
        "",
        "",
        "# ----------------------------------------------------------------- rules",
        "",
        "def due_day(day_out, loan_days=14):",
        "    ...",
        "",
        "",
        "def fine_for(day_due, day_returned, per_day=50, cap=2000):",
        "    ...",
        "",
        "",
        "# --------------------------------------------------------------- service",
        "",
        "class Library:",
        "    MAX_LOANS = 3",
        "",
        "    def __init__(self, path=':memory:'):",
        "        self.con = sqlite3.connect(path)",
        "        self.setup()"
      ]),
      outro: L([
        "**You have finished the journey.**",
        "",
        "Look at what that last stage actually asked: you wrote a test suite, and it was run against three",
        "deliberately broken implementations to prove it would catch them. Not *does your code work* —",
        "*can you demonstrate that you would know if it stopped working*.",
        "",
        "Along the way you built a domain model that cannot hold invalid data, pure rules you can test in one",
        "line, a normalised schema with parameterised queries, a service layer that coordinates them, and a",
        "report — from a specification, with no worked example to copy.",
        "",
        "That is not *completing a course*. That is the job.",
        "",
        "**Where to go now:** build something nobody set you. Pick a problem you actually have — a script that",
        "tidies your files, a tracker for something you care about, a small web API — and build it with the",
        "same layering, the same tests, the same honesty about failure. The gap between finishing a curriculum",
        "and being a programmer is closed by exactly one thing: shipping something you chose yourself.",
        "",
        "Keep the review sessions going. Knowledge that is not retrieved fades, and you have earned this."
      ]),
      stages: [
        {
          title: "The domain model",
          xp: 150,
          spec: L([
            "Two frozen dataclasses that refuse to hold invalid data.",
            "",
            "**`Book`** — fields `isbn: str`, `title: str`, `copies: int = 1`",
            "",
            "- `isbn` must be a non-empty string after stripping",
            "- `title` must be non-empty after stripping",
            "- `copies` must be at least 1",
            "- otherwise raise `ValueError`",
            "",
            "**`Member`** — fields `member_id: str`, `name: str`",
            "",
            "- both must be non-empty after stripping, otherwise `ValueError`",
            "",
            "Both are `@dataclass(frozen=True)`, so validation goes in `__post_init__`.",
            "Being frozen, they must also be usable as dictionary keys."
          ]),
          tests: [
            { name: "a valid book", code: "b = Book('123', 'Dune', 3)\nassert b.isbn == '123' and b.title == 'Dune' and b.copies == 3" },
            { name: "copies defaults to 1", code: "assert Book('1', 'T').copies == 1" },
            { name: "empty isbn is refused", code: "for bad in ['', '   ']:\n    try:\n        Book(bad, 'T')\n        raise AssertionError(f'{bad!r} should raise')\n    except ValueError:\n        pass" },
            { name: "empty title is refused", code: "try:\n    Book('1', '  ')\n    raise AssertionError('should raise')\nexcept ValueError:\n    pass" },
            { name: "zero copies is refused", code: "for bad in [0, -1]:\n    try:\n        Book('1', 'T', bad)\n        raise AssertionError('should raise')\n    except ValueError:\n        pass" },
            { name: "a valid member", code: "m = Member('m1', 'Ada')\nassert m.member_id == 'm1' and m.name == 'Ada'" },
            { name: "empty member fields are refused", code: "for args in [('', 'Ada'), ('m1', '  ')]:\n    try:\n        Member(*args)\n        raise AssertionError(f'{args} should raise')\n    except ValueError:\n        pass" },
            { name: "frozen: assignment fails", code: "b = Book('1', 'T')\ntry:\n    b.title = 'Other'\n    raise AssertionError('should be immutable')\nexcept Exception:\n    pass" },
            { name: "usable as dict keys", code: "d = {Book('1', 'T'): 5}\nassert d[Book('1', 'T')] == 5" },
            { name: "equality by value", code: "assert Book('1', 'T', 2) == Book('1', 'T', 2)\nassert Member('m', 'A') == Member('m', 'A')" }
          ]
        },
        {
          title: "The rules",
          xp: 150,
          spec: L([
            "Three pure functions. No I/O, no clock, no database.",
            "",
            "**`due_day(day_out, loan_days=14)`** → the day the loan falls due.",
            "`loan_days` must be at least 1, otherwise `ValueError`.",
            "",
            "**`fine_for(day_due, day_returned, per_day=50, cap=2000)`** → the fine in pence.",
            "Zero if returned on or before the due day; otherwise `per_day` per day late, never above `cap`.",
            "",
            "**`is_overdue(day_due, today, day_returned=None)`** → `True` only when the book has **not** been",
            "returned and `today` is past the due day. A returned book is never overdue."
          ]),
          tests: [
            { name: "due day", code: "assert due_day(100) == 114\nassert due_day(100, 7) == 107" },
            { name: "loan_days must be positive", code: "for bad in [0, -3]:\n    try:\n        due_day(100, bad)\n        raise AssertionError('should raise')\n    except ValueError:\n        pass" },
            { name: "no fine when on time", code: "assert fine_for(100, 100) == 0 and fine_for(100, 90) == 0" },
            { name: "fine per day late", code: "assert fine_for(100, 101) == 50 and fine_for(100, 104) == 200" },
            { name: "fine is capped", code: "assert fine_for(100, 500) == 2000\nassert fine_for(100, 500, cap=300) == 300" },
            { name: "rate is configurable", code: "assert fine_for(100, 103, per_day=25) == 75" },
            { name: "overdue only when unreturned", code: "assert is_overdue(100, 101) is True\nassert is_overdue(100, 100) is False\nassert is_overdue(100, 99) is False" },
            { name: "a returned book is never overdue", code: "assert is_overdue(100, 200, day_returned=150) is False\nassert is_overdue(100, 200, day_returned=99) is False" },
            { name: "the rules are pure", code: "import io, sys\nbuf = io.StringIO()\nold = sys.stdout\nsys.stdout = buf\ndue_day(1); fine_for(1, 2); is_overdue(1, 2)\nsys.stdout = old\nassert buf.getvalue() == '', 'rules must not print'" }
          ]
        },
        {
          title: "Storage",
          xp: 200,
          spec: L([
            "Implement `Library.setup()` and the storage methods.",
            "",
            "**Schema** — three tables, created with `IF NOT EXISTS`:",
            "",
            "- `books(isbn TEXT PRIMARY KEY, title TEXT NOT NULL, copies INTEGER NOT NULL)`",
            "- `members(member_id TEXT PRIMARY KEY, name TEXT NOT NULL)`",
            "- `loans(id INTEGER PRIMARY KEY, isbn TEXT NOT NULL, member_id TEXT NOT NULL,",
            "  day_out INTEGER NOT NULL, day_due INTEGER NOT NULL, day_returned INTEGER)`",
            "",
            "**Methods:**",
            "",
            "- `add_book(book)` — stores a `Book`; adding the same isbn again raises `ValueError`",
            "- `add_member(member)` — stores a `Member`; a duplicate id raises `ValueError`",
            "- `get_book(isbn)` → a `Book` or `None`",
            "- `get_member(member_id)` → a `Member` or `None`",
            "- `active_loans(member_id)` → the number of that member's unreturned loans",
            "- `available(isbn)` → copies minus unreturned loans of that book; `0` for an unknown book",
            "",
            "All queries must be parameterised."
          ]),
          tests: [
            { name: "tables exist", code: "lib = Library()\nnames = {r[0] for r in lib.con.execute(\"SELECT name FROM sqlite_master WHERE type='table'\")}\nassert {'books', 'members', 'loans'} <= names, names" },
            { name: "add and read a book", code: "lib = Library()\nlib.add_book(Book('1', 'Dune', 2))\nassert lib.get_book('1') == Book('1', 'Dune', 2)" },
            { name: "unknown book is None", code: "assert Library().get_book('nope') is None" },
            { name: "duplicate isbn raises", code: "lib = Library()\nlib.add_book(Book('1', 'Dune'))\ntry:\n    lib.add_book(Book('1', 'Other'))\n    raise AssertionError('should raise')\nexcept ValueError:\n    pass" },
            { name: "add and read a member", code: "lib = Library()\nlib.add_member(Member('m1', 'Ada'))\nassert lib.get_member('m1') == Member('m1', 'Ada')" },
            { name: "duplicate member raises", code: "lib = Library()\nlib.add_member(Member('m1', 'Ada'))\ntry:\n    lib.add_member(Member('m1', 'Bo'))\n    raise AssertionError('should raise')\nexcept ValueError:\n    pass" },
            { name: "availability starts at the copy count", code: "lib = Library()\nlib.add_book(Book('1', 'Dune', 3))\nassert lib.available('1') == 3" },
            { name: "unknown book has no availability", code: "assert Library().available('nope') == 0" },
            { name: "no active loans initially", code: "lib = Library()\nlib.add_member(Member('m1', 'Ada'))\nassert lib.active_loans('m1') == 0" },
            { name: "setup is repeatable", code: "lib = Library()\nlib.setup()\nlib.add_book(Book('1', 'T'))\nassert lib.available('1') == 1" },
            { name: "queries are parameterised", code: "assert '?' in _SRC, 'use parameterised queries'" }
          ]
        },
        {
          title: "Borrowing and returning",
          xp: 250,
          spec: L([
            "The service layer. Both methods take the current day explicitly.",
            "",
            "**`borrow(isbn, member_id, today, loan_days=14)`** → the new loan id (an `int`).",
            "",
            "Raises `ValueError` when:",
            "",
            "- the book does not exist",
            "- the member does not exist",
            "- no copies are available",
            "- the member already has `MAX_LOANS` (3) unreturned loans",
            "",
            "The message must name the reason. Nothing may be written when it raises.",
            "",
            "**`return_book(loan_id, today)`** → the fine in pence (an `int`).",
            "",
            "- raises `ValueError` for an unknown loan id, or one already returned",
            "- records `day_returned`, and the copy becomes available again",
            "- the fine comes from `fine_for`"
          ]),
          tests: [
            {
              name: "a successful borrow",
              code: "lib = Library()\nlib.add_book(Book('1', 'Dune', 2))\nlib.add_member(Member('m1', 'Ada'))\nloan = lib.borrow('1', 'm1', 100)\nassert isinstance(loan, int)\nassert lib.available('1') == 1\nassert lib.active_loans('m1') == 1"
            },
            {
              name: "unknown book or member raises",
              code: "lib = Library()\nlib.add_book(Book('1', 'Dune'))\nlib.add_member(Member('m1', 'Ada'))\nfor args in [('nope', 'm1'), ('1', 'nobody')]:\n    try:\n        lib.borrow(args[0], args[1], 100)\n        raise AssertionError(f'{args} should raise')\n    except ValueError:\n        pass"
            },
            {
              name: "no copies left",
              code: "lib = Library()\nlib.add_book(Book('1', 'Dune', 1))\nlib.add_member(Member('m1', 'Ada'))\nlib.add_member(Member('m2', 'Bo'))\nlib.borrow('1', 'm1', 100)\ntry:\n    lib.borrow('1', 'm2', 100)\n    raise AssertionError('should raise')\nexcept ValueError:\n    pass"
            },
            {
              name: "the loan limit is enforced",
              code: "lib = Library()\nlib.add_member(Member('m1', 'Ada'))\nfor i in range(4):\n    lib.add_book(Book(str(i), f'Book {i}'))\nfor i in range(3):\n    lib.borrow(str(i), 'm1', 100)\ntry:\n    lib.borrow('3', 'm1', 100)\n    raise AssertionError('the fourth loan should be refused')\nexcept ValueError:\n    pass\nassert lib.active_loans('m1') == 3"
            },
            {
              name: "a refused borrow writes nothing",
              code: "lib = Library()\nlib.add_book(Book('1', 'Dune', 1))\nlib.add_member(Member('m1', 'Ada'))\nlib.borrow('1', 'm1', 100)\nbefore = lib.con.execute('SELECT COUNT(*) FROM loans').fetchone()[0]\ntry:\n    lib.borrow('1', 'm1', 100)\nexcept ValueError:\n    pass\nafter = lib.con.execute('SELECT COUNT(*) FROM loans').fetchone()[0]\nassert before == after, 'no loan row should be created when it raises'"
            },
            {
              name: "returning on time costs nothing",
              code: "lib = Library()\nlib.add_book(Book('1', 'Dune'))\nlib.add_member(Member('m1', 'Ada'))\nloan = lib.borrow('1', 'm1', 100)\nassert lib.return_book(loan, 110) == 0\nassert lib.available('1') == 1\nassert lib.active_loans('m1') == 0"
            },
            {
              name: "returning late costs the fine",
              code: "lib = Library()\nlib.add_book(Book('1', 'Dune'))\nlib.add_member(Member('m1', 'Ada'))\nloan = lib.borrow('1', 'm1', 100)\nassert lib.return_book(loan, 118) == 200, 'due on 114, returned on 118 = 4 days x 50p'"
            },
            {
              name: "returning twice raises",
              code: "lib = Library()\nlib.add_book(Book('1', 'Dune'))\nlib.add_member(Member('m1', 'Ada'))\nloan = lib.borrow('1', 'm1', 100)\nlib.return_book(loan, 110)\ntry:\n    lib.return_book(loan, 111)\n    raise AssertionError('should raise')\nexcept ValueError:\n    pass"
            },
            {
              name: "unknown loan raises",
              code: "try:\n    Library().return_book(999, 100)\n    raise AssertionError('should raise')\nexcept ValueError:\n    pass"
            },
            {
              name: "a returned copy can be borrowed again",
              code: "lib = Library()\nlib.add_book(Book('1', 'Dune', 1))\nlib.add_member(Member('m1', 'Ada'))\nlib.add_member(Member('m2', 'Bo'))\nloan = lib.borrow('1', 'm1', 100)\nlib.return_book(loan, 105)\nsecond = lib.borrow('1', 'm2', 106)\nassert isinstance(second, int) and second != loan"
            }
          ]
        },
        {
          title: "Reporting",
          xp: 250,
          spec: L([
            "Two read-only views over the data.",
            "",
            "**`overdue(today)`** → a list of dictionaries, one per unreturned overdue loan, each with",
            "`loan_id`, `isbn`, `title`, `member_id`, `name`, `days_late` and `fine`.",
            "",
            "Sorted by `days_late` descending, then by `loan_id` ascending. Use a single query with joins —",
            "not one query per loan.",
            "",
            "**`report(today)`** → a formatted string:",
            "",
            "~~~text",
            "Library report (day 130)",
            "Books: 2  Members: 2  On loan: 2",
            "",
            "Overdue",
            "  Dune            Ada        16d  £8.00",
            "  Neuromancer     Bo          2d  £1.00",
            "~~~",
            "",
            "",
            "Rules:",
            "",
            "- line 1 names the day exactly as shown",
            "- line 2 counts distinct books, members, and currently unreturned loans",
            "- line 3 is blank, line 4 is `Overdue`",
            "- each overdue line is two spaces, then the title left-aligned in 16 characters, then the member",
            "  name left-aligned in 10, then the days late right-aligned in 3 with a `d` suffix, two spaces,",
            "  then the fine as `£` and pounds to two decimals",
            "- when nothing is overdue, line 4 is `Overdue` and line 5 is `  (none)`",
            "- no trailing newline"
          ]),
          tests: [
            {
              name: "overdue list is correct",
              code: "lib = Library()\nlib.add_book(Book('1', 'Dune'))\nlib.add_member(Member('m1', 'Ada'))\nloan = lib.borrow('1', 'm1', 100)\nrows = lib.overdue(130)\nassert len(rows) == 1, rows\nrow = rows[0]\nassert row['title'] == 'Dune' and row['name'] == 'Ada'\nassert row['days_late'] == 16 and row['fine'] == 800\nassert row['loan_id'] == loan and row['isbn'] == '1' and row['member_id'] == 'm1'"
            },
            {
              name: "returned loans are not overdue",
              code: "lib = Library()\nlib.add_book(Book('1', 'Dune'))\nlib.add_member(Member('m1', 'Ada'))\nloan = lib.borrow('1', 'm1', 100)\nlib.return_book(loan, 200)\nassert lib.overdue(300) == []"
            },
            {
              name: "not yet due is not overdue",
              code: "lib = Library()\nlib.add_book(Book('1', 'Dune'))\nlib.add_member(Member('m1', 'Ada'))\nlib.borrow('1', 'm1', 100)\nassert lib.overdue(114) == []\nassert len(lib.overdue(115)) == 1"
            },
            {
              name: "sorted by days late descending",
              code: "lib = Library()\nlib.add_member(Member('m1', 'Ada'))\nlib.add_book(Book('1', 'Old'))\nlib.add_book(Book('2', 'New'))\nlib.borrow('1', 'm1', 100)\nlib.borrow('2', 'm1', 120)\nrows = lib.overdue(140)\nassert [r['title'] for r in rows] == ['Old', 'New'], [r['title'] for r in rows]"
            },
            {
              name: "the report format is exact",
              code: "lib = Library()\nlib.add_book(Book('1', 'Dune'))\nlib.add_book(Book('2', 'Neuromancer'))\nlib.add_member(Member('m1', 'Ada'))\nlib.add_member(Member('m2', 'Bo'))\nlib.borrow('1', 'm1', 100)\nlib.borrow('2', 'm2', 114)\nexpected = 'Library report (day 130)\\nBooks: 2  Members: 2  On loan: 2\\n\\nOverdue\\n  Dune            Ada        16d  £8.00\\n  Neuromancer     Bo          2d  £1.00'\nactual = lib.report(130)\nassert actual == expected, 'got:\\n' + actual"
            },
            {
              name: "nothing overdue",
              code: "lib = Library()\nlib.add_book(Book('1', 'Dune'))\nlib.add_member(Member('m1', 'Ada'))\nexpected = 'Library report (day 100)\\nBooks: 1  Members: 1  On loan: 0\\n\\nOverdue\\n  (none)'\nactual = lib.report(100)\nassert actual == expected, 'got:\\n' + actual"
            },
            {
              name: "on-loan count excludes returned books",
              code: "lib = Library()\nlib.add_book(Book('1', 'Dune', 2))\nlib.add_member(Member('m1', 'Ada'))\nloan = lib.borrow('1', 'm1', 100)\nlib.borrow('1', 'm1', 100)\nlib.return_book(loan, 101)\nassert 'On loan: 1' in lib.report(102)"
            },
            {
              name: "overdue uses one query, not one per loan",
              code: "lib = Library()\nlib.add_member(Member('m1', 'Ada'))\nfor i in range(3):\n    lib.add_book(Book(str(i), f'B{i}'))\n    lib.borrow(str(i), 'm1', 100)\nstatements = []\nlib.con.set_trace_callback(statements.append)\ntry:\n    rows = lib.overdue(140)\nfinally:\n    lib.con.set_trace_callback(None)\nassert len(rows) == 3\nqueries = [s for s in statements if 'SELECT' in s.upper()]\nassert len(queries) <= 2, f'overdue() ran {len(queries)} SELECT queries — use a join'"
            }
          ]
        },
        {
          title: "Prove it",
          xp: 200,
          spec: L([
            "The final stage. **You do not write more system code — you write the tests.**",
            "",
            "Add at least **five** test functions to your file. Each must:",
            "",
            "- be named `test_<something>`",
            "- take exactly **one parameter**: the `fine_for` implementation to test",
            "- use `assert`",
            "",
            "~~~py",
            "def test_returned_on_due_day_is_free(fine_for):",
            "    assert fine_for(100, 100) == 0",
            "~~~",
            "",
            "Your suite must pass against your correct `fine_for`, and it must **fail** against each of three",
            "deliberately broken versions:",
            "",
            "1. one that charges from the day *before* the due day (an off-by-one)",
            "2. one with no cap",
            "3. one that returns a negative fine for early returns",
            "",
            "All five earlier stages must still pass.",
            "",
            ":::tip This is the exit bar",
            "A suite that passes correct code and also passes broken code is worse than none, because it creates",
            "confidence that is not earned. Think about which single input would expose each bug.",
            ":::"
          ]),
          tests: [
            {
              name: "at least five single-parameter test functions",
              code: "import inspect\ntests = [v for k, v in list(globals().items()) if k.startswith('test_') and callable(v)]\nassert len(tests) >= 5, f'found {len(tests)} test functions'\nfor t in tests:\n    params = inspect.signature(t).parameters\n    assert len(params) == 1, f'{t.__name__} must take exactly one parameter'"
            },
            {
              name: "your tests pass against a correct implementation",
              code: "def good(day_due, day_returned, per_day=50, cap=2000):\n    late = day_returned - day_due\n    if late <= 0:\n        return 0\n    return min(late * per_day, cap)\ntests = [v for k, v in list(globals().items()) if k.startswith('test_') and callable(v)]\nfor t in tests:\n    t(good)"
            },
            {
              name: "your tests catch the off-by-one",
              code: "def off_by_one(day_due, day_returned, per_day=50, cap=2000):\n    late = day_returned - day_due + 1\n    if late <= 0:\n        return 0\n    return min(late * per_day, cap)\ntests = [v for k, v in list(globals().items()) if k.startswith('test_') and callable(v)]\ncaught = False\nfor t in tests:\n    try:\n        t(off_by_one)\n    except AssertionError:\n        caught = True\nassert caught, 'no test caught a fine charged one day early'"
            },
            {
              name: "your tests catch the missing cap",
              code: "def no_cap(day_due, day_returned, per_day=50, cap=2000):\n    late = day_returned - day_due\n    if late <= 0:\n        return 0\n    return late * per_day\ntests = [v for k, v in list(globals().items()) if k.startswith('test_') and callable(v)]\ncaught = False\nfor t in tests:\n    try:\n        t(no_cap)\n    except AssertionError:\n        caught = True\nassert caught, 'no test checked that the fine is capped'"
            },
            {
              name: "your tests catch the negative fine",
              code: "def negative(day_due, day_returned, per_day=50, cap=2000):\n    return min((day_returned - day_due) * per_day, cap)\ntests = [v for k, v in list(globals().items()) if k.startswith('test_') and callable(v)]\ncaught = False\nfor t in tests:\n    try:\n        t(negative)\n    except AssertionError:\n        caught = True\nassert caught, 'no test covered an early return'"
            },
            {
              name: "the whole system still works end to end",
              code: "lib = Library()\nlib.add_book(Book('1', 'Dune', 2))\nlib.add_member(Member('m1', 'Ada'))\nloan = lib.borrow('1', 'm1', 100)\nassert lib.available('1') == 1\nassert len(lib.overdue(130)) == 1\nassert lib.return_book(loan, 130) == 800\nassert lib.overdue(130) == []\nassert 'On loan: 0' in lib.report(130)"
            },
            {
              name: "the rules are still correct",
              code: "assert due_day(100) == 114\nassert fine_for(100, 100) == 0\nassert fine_for(100, 101) == 50\nassert fine_for(100, 9999) == 2000\nassert is_overdue(100, 101) is True"
            },
            {
              name: "the domain model still validates",
              code: "try:\n    Book('', 'T')\n    raise AssertionError('should raise')\nexcept ValueError:\n    pass\nassert Book('1', 'T', 2) == Book('1', 'T', 2)"
            }
          ]
        }
      ],
      solution: L([
        "\"\"\"The Lending Library — reference solution.\"\"\"",
        "import sqlite3",
        "from dataclasses import dataclass",
        "",
        "",
        "# ---------------------------------------------------------------- domain",
        "",
        "@dataclass(frozen=True)",
        "class Book:",
        "    isbn: str",
        "    title: str",
        "    copies: int = 1",
        "",
        "    def __post_init__(self):",
        "        if not isinstance(self.isbn, str) or not self.isbn.strip():",
        "            raise ValueError('isbn must be a non-empty string')",
        "        if not isinstance(self.title, str) or not self.title.strip():",
        "            raise ValueError('title must be a non-empty string')",
        "        if not isinstance(self.copies, int) or self.copies < 1:",
        "            raise ValueError(f'copies must be at least 1, got {self.copies}')",
        "",
        "",
        "@dataclass(frozen=True)",
        "class Member:",
        "    member_id: str",
        "    name: str",
        "",
        "    def __post_init__(self):",
        "        if not isinstance(self.member_id, str) or not self.member_id.strip():",
        "            raise ValueError('member_id must be a non-empty string')",
        "        if not isinstance(self.name, str) or not self.name.strip():",
        "            raise ValueError('name must be a non-empty string')",
        "",
        "",
        "# ----------------------------------------------------------------- rules",
        "",
        "def due_day(day_out, loan_days=14):",
        "    if loan_days < 1:",
        "        raise ValueError(f'loan_days must be at least 1, got {loan_days}')",
        "    return day_out + loan_days",
        "",
        "",
        "def fine_for(day_due, day_returned, per_day=50, cap=2000):",
        "    days_late = day_returned - day_due",
        "    if days_late <= 0:",
        "        return 0",
        "    return min(days_late * per_day, cap)",
        "",
        "",
        "def is_overdue(day_due, today, day_returned=None):",
        "    if day_returned is not None:",
        "        return False",
        "    return today > day_due",
        "",
        "",
        "# --------------------------------------------------------------- service",
        "",
        "class Library:",
        "    MAX_LOANS = 3",
        "",
        "    def __init__(self, path=':memory:'):",
        "        self.con = sqlite3.connect(path)",
        "        self.setup()",
        "",
        "    def setup(self):",
        "        self.con.execute('''",
        "            CREATE TABLE IF NOT EXISTS books (",
        "                isbn   TEXT PRIMARY KEY,",
        "                title  TEXT NOT NULL,",
        "                copies INTEGER NOT NULL",
        "            )''')",
        "        self.con.execute('''",
        "            CREATE TABLE IF NOT EXISTS members (",
        "                member_id TEXT PRIMARY KEY,",
        "                name      TEXT NOT NULL",
        "            )''')",
        "        self.con.execute('''",
        "            CREATE TABLE IF NOT EXISTS loans (",
        "                id           INTEGER PRIMARY KEY,",
        "                isbn         TEXT NOT NULL,",
        "                member_id    TEXT NOT NULL,",
        "                day_out      INTEGER NOT NULL,",
        "                day_due      INTEGER NOT NULL,",
        "                day_returned INTEGER",
        "            )''')",
        "        self.con.commit()",
        "",
        "    # ---------------------------------------------------------- storage",
        "",
        "    def add_book(self, book):",
        "        try:",
        "            self.con.execute(",
        "                'INSERT INTO books (isbn, title, copies) VALUES (?, ?, ?)',",
        "                (book.isbn, book.title, book.copies))",
        "        except sqlite3.IntegrityError as error:",
        "            raise ValueError(f'book already exists: {book.isbn}') from error",
        "        self.con.commit()",
        "",
        "    def add_member(self, member):",
        "        try:",
        "            self.con.execute(",
        "                'INSERT INTO members (member_id, name) VALUES (?, ?)',",
        "                (member.member_id, member.name))",
        "        except sqlite3.IntegrityError as error:",
        "            raise ValueError(f'member already exists: {member.member_id}') from error",
        "        self.con.commit()",
        "",
        "    def get_book(self, isbn):",
        "        row = self.con.execute(",
        "            'SELECT isbn, title, copies FROM books WHERE isbn = ?', (isbn,)).fetchone()",
        "        return Book(row[0], row[1], row[2]) if row else None",
        "",
        "    def get_member(self, member_id):",
        "        row = self.con.execute(",
        "            'SELECT member_id, name FROM members WHERE member_id = ?', (member_id,)).fetchone()",
        "        return Member(row[0], row[1]) if row else None",
        "",
        "    def active_loans(self, member_id):",
        "        return self.con.execute(",
        "            'SELECT COUNT(*) FROM loans WHERE member_id = ? AND day_returned IS NULL',",
        "            (member_id,)).fetchone()[0]",
        "",
        "    def available(self, isbn):",
        "        book = self.get_book(isbn)",
        "        if book is None:",
        "            return 0",
        "        out = self.con.execute(",
        "            'SELECT COUNT(*) FROM loans WHERE isbn = ? AND day_returned IS NULL',",
        "            (isbn,)).fetchone()[0]",
        "        return book.copies - out",
        "",
        "    # ---------------------------------------------------------- service",
        "",
        "    def borrow(self, isbn, member_id, today, loan_days=14):",
        "        if self.get_book(isbn) is None:",
        "            raise ValueError(f'unknown book: {isbn}')",
        "        if self.get_member(member_id) is None:",
        "            raise ValueError(f'unknown member: {member_id}')",
        "        if self.available(isbn) < 1:",
        "            raise ValueError(f'no copies available: {isbn}')",
        "        if self.active_loans(member_id) >= self.MAX_LOANS:",
        "            raise ValueError(f'loan limit reached for {member_id}')",
        "",
        "        cur = self.con.execute(",
        "            'INSERT INTO loans (isbn, member_id, day_out, day_due) VALUES (?, ?, ?, ?)',",
        "            (isbn, member_id, today, due_day(today, loan_days)))",
        "        self.con.commit()",
        "        return cur.lastrowid",
        "",
        "    def return_book(self, loan_id, today):",
        "        row = self.con.execute(",
        "            'SELECT day_due, day_returned FROM loans WHERE id = ?', (loan_id,)).fetchone()",
        "        if row is None:",
        "            raise ValueError(f'unknown loan: {loan_id}')",
        "        if row[1] is not None:",
        "            raise ValueError(f'loan {loan_id} was already returned')",
        "",
        "        self.con.execute(",
        "            'UPDATE loans SET day_returned = ? WHERE id = ?', (today, loan_id))",
        "        self.con.commit()",
        "        return fine_for(row[0], today)",
        "",
        "    # -------------------------------------------------------- reporting",
        "",
        "    def overdue(self, today):",
        "        rows = self.con.execute('''",
        "            SELECT loans.id, loans.isbn, books.title,",
        "                   loans.member_id, members.name, loans.day_due",
        "            FROM loans",
        "            JOIN books ON books.isbn = loans.isbn",
        "            JOIN members ON members.member_id = loans.member_id",
        "            WHERE loans.day_returned IS NULL AND loans.day_due < ?",
        "        ''', (today,)).fetchall()",
        "",
        "        result = []",
        "        for loan_id, isbn, title, member_id, name, day_due in rows:",
        "            result.append({",
        "                'loan_id': loan_id,",
        "                'isbn': isbn,",
        "                'title': title,",
        "                'member_id': member_id,",
        "                'name': name,",
        "                'days_late': today - day_due,",
        "                'fine': fine_for(day_due, today),",
        "            })",
        "        result.sort(key=lambda row: (-row['days_late'], row['loan_id']))",
        "        return result",
        "",
        "    def report(self, today):",
        "        books = self.con.execute('SELECT COUNT(*) FROM books').fetchone()[0]",
        "        members = self.con.execute('SELECT COUNT(*) FROM members').fetchone()[0]",
        "        on_loan = self.con.execute(",
        "            'SELECT COUNT(*) FROM loans WHERE day_returned IS NULL').fetchone()[0]",
        "",
        "        lines = [",
        "            f'Library report (day {today})',",
        "            f'Books: {books}  Members: {members}  On loan: {on_loan}',",
        "            '',",
        "            'Overdue',",
        "        ]",
        "",
        "        rows = self.overdue(today)",
        "        if not rows:",
        "            lines.append('  (none)')",
        "        else:",
        "            for row in rows:",
        "                days = f\"{row['days_late']}d\"",
        "                money = f\"£{row['fine'] / 100:.2f}\"",
        "                lines.append(f\"  {row['title']:<16}{row['name']:<10}{days:>4}  {money}\")",
        "",
        "        return '\\n'.join(lines)",
        "",
        "",
        "# ----------------------------------------------------------------- tests",
        "",
        "def test_returned_on_the_due_day_is_free(fine_for):",
        "    assert fine_for(100, 100) == 0",
        "",
        "",
        "def test_returned_early_is_free(fine_for):",
        "    assert fine_for(100, 90) == 0",
        "",
        "",
        "def test_one_day_late_costs_one_day(fine_for):",
        "    assert fine_for(100, 101) == 50",
        "",
        "",
        "def test_several_days_late(fine_for):",
        "    assert fine_for(100, 104) == 200",
        "",
        "",
        "def test_the_fine_is_capped(fine_for):",
        "    assert fine_for(100, 10000) == 2000",
        "",
        "",
        "def test_the_fine_is_never_negative(fine_for):",
        "    assert all(fine_for(100, day) >= 0 for day in range(80, 120))"
      ])
    }
  });
})();
